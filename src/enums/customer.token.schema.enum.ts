export enum UserTokenStatusEnum {
    ACTIVE = 'active',
    EXPIRED = 'expired',
}
export enum UserTokenTypeEnum {
    REFRESH = 'refresh',
    SET_PASSWORD = 'set-password',
    ACCESS = 'access',
}
